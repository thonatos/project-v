{
	"devDependencies": {
		"@commitlint/cli": "^20.5.0",
		"@commitlint/config-conventional": "^20.5.0",
		"husky": "^9.1.7",
		"oxfmt": "^0.46.0",
		"oxlint": "^1.61.0"
	},
	"scripts": {
		"prepare": "husky",
		"lint": "oxlint lint packages",
		"lint:fix": "oxlint lint packages --fix",
		"format": "oxfmt format packages --check",
		"format:write": "oxfmt format packages --write"
	}
}
